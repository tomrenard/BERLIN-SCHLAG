<div class="header-ber">
<div class="navbar-berlin">
  <div class="dropdown">
  <button class="dropbtn"><i class="fas fa-grip-lines"></i></button>
  <div class="dropdown-content">
    <% if user_signed_in? %>
    <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "underline" %>
    <% else %>
    <%= link_to "Login (Gros Shlag Only)", new_user_session_path %>
    <% end %>
  </div>
</div>
  <div class="logo-container">
    <div class="logo">
  <a href="/">BerlinSchlag</a>
    </div>
     <div class="progress-bar" id="myBar"></div>
  </div>
  <% if user_signed_in? %>
  <div class="social">
    <%= link_to new_article_path do %>
      <i class="fas fa-plus"></i>
     <% end %>
  </div>
  <% else %>
   <div class="social">
    <a href='/'>Logo</a>
  </div>
   <% end %>
</div>
</div>
